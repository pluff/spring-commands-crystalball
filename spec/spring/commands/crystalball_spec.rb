require "spec_helper"

RSpec.describe Spring::Commands::Crystalball do
  it "has a version number" do
    expect(Spring::Commands::Crystalball::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
